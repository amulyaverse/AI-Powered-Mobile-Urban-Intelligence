# Backend — AI-Powered Mobile Urban Intelligence Platform
# Install with: pip install -r requirements.txt

# Web framework
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# Database ORM + migrations
sqlalchemy>=2.0.0
alembic>=1.13.0

# PostgreSQL async driver
psycopg2-binary>=2.9.9       # sync driver (for Alembic + sync SQLAlchemy)

# Pydantic v2 (bundled with FastAPI, pinned for compatibility)
pydantic>=2.7.0
pydantic-settings>=2.2.0     # for BaseSettings config

# Environment / config
python-dotenv>=1.0.0

# HTTP client (for integration tests)
httpx>=0.27.0

# Utilities
python-dateutil>=2.9.0       # robust date parsing

# Testing
pytest>=8.2.0
pytest-asyncio>=0.23.0
